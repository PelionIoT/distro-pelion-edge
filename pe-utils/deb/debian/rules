#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
# export DH_VERBOSE = 1

%:
	dh $@

override_dh_installsystemd:
	dh_installsystemd --name=wait-for-pelion-identity wait-for-pelion-identity.service
