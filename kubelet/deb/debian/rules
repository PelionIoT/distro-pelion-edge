#!/usr/bin/make -f

# DPKG_EXPORT_BUILDFLAGS = 1
# include /usr/share/dpkg/buildflags.mk

%:
	dh $@

override_dh_auto_build:
	./debian/auto_build

override_dh_auto_clean:
	-rm -rf go-workspace
	-rm -f  kubelet

override_dh_auto_test:
# TODO: By default it calls "make test" and the tests fail.
# Something about libfakeroot-sysv.so. Turning it off for now.
